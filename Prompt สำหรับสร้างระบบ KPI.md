**Role:**  
ทำหน้าที่เป็น Full-Stack Web Developer และ UI/UX Designer ที่เชี่ยวชาญการใช้ **Next.js (App Router), TypeScript, Tailwind CSS, shadcn/ui, Supabase** มีทักษะโดดเด่นด้านการออกแบบสไตล์ **"Sleek Design"** (หรูหรา ทันสมัย มินิมอล และลื่นไหล) สำหรับระบบระดับองค์กร (Premium Enterprise Dashboard)  
**Project Overview:** ต้องการสร้าง Web Application สำหรับ "ระบบรายงานผลการดำเนินงานตามเกณฑ์วัดของสายงาน" สำหรับ **ฝ่ายบริหารจัดการสินทรัพย์ระบบไฟฟ้า (ฝบร.)** โดยระบบจะมี **Workflow การอนุมัติ 3 ระดับ** สำหรับการเพิ่มหัวข้อตัวชี้วัดและการรายงานผล  
**Organization Structure (โครงสร้างองค์กรอ้างอิงจากรูปภาพ):** ฝ่ายบริหารจัดการสินทรัพย์ระบบไฟฟ้า (ฝบร.) ประกอบด้วย 3 กองหลัก ได้แก่:

1. กองจัดการงานบำรุงรักษาระบบผลิต (กบผ.)  
2. กองบริหารจัดการระบบไฟฟ้า (กบร.)  
3. กองบริหารจัดการงานเครื่องกล (กบค.)

**User Roles & Permissions (ระบบสิทธิผู้ใช้งานด้วย Supabase Auth):** ระบบต้องมีจัดการ Role-based Access Control (RBAC) โดยแบ่งสิทธิเป็น 5 ระดับ ดังนี้:

1. **ผู้อำนวยการฝ่าย ฝบร. (Department Director):** สิทธิระดับบนสุด ทำหน้าที่เป็น **ผู้อนุมัติระดับที่ 3 (Final Approver)** ดูข้อมูลได้ทุกกอง  
2. **ผู้รวบรวมส่งผู้บริหาร/เลขาฝ่าย (Consolidator):** ทำหน้าที่เป็น **ผู้อนุมัติ/กลั่นกรองระดับที่ 2** ตรวจสอบความถูกต้องของข้อมูลทุกกองก่อนส่งให้ผู้อำนวยการฝ่าย  
3. **ผู้อำนวยการกอง (Division Director):** ทำหน้าที่เป็น **ผู้อนุมัติระดับที่ 1** (จัดการและอนุมัติข้อมูลได้เฉพาะกองตนเอง)  
4. **ผู้ได้รับมอบหมายระดับกอง (Assignee / Maker):** ผู้จัดทำข้อมูล มีสิทธิ สร้าง/แก้ไขหัวข้อ KPI (โดยมีการกำหนดเป้าหมาย ระดับ 1-5) และกรอกรายงานผลของ "กองตนเอง" เพื่อกด **"ส่งขออนุมัติ"**  
5. **พนักงานทั่วไปของกอง (Division Staff):** สามารถ Login เข้าดู Dashboard และรายงานผลตัวชี้วัดของ "กองตนเอง" ได้แบบ Read-only (ไม่สามารถแก้ไขหรืออนุมัติได้)

**Approval Workflow (ระบบสายการอนุมัติ):** นำไปใช้กับทั้ง 1\) การสร้าง/แก้ไขหัวข้อตัวชี้วัด และ 2\) การรายงานผลรายเดือน

* **สถานะของเอกสาร (Status):** ฉบับร่าง (Draft) \-\> รอ ผอ.กอง อนุมัติ (Pending Level 1\) \-\> รอผู้รวบรวมฯ ตรวจสอบ (Pending Level 2\) \-\> รอ ผอ.ฝ่าย อนุมัติ (Pending Level 3\) \-\> อนุมัติแล้ว (Approved)  
* **Actions:** ผู้อนุมัติในแต่ละระดับ เมื่อ Login เข้ามาจะเห็นปุ่ม **"อนุมัติ (Approve)"** หรือ **"ส่งกลับแก้ไข (Reject/Revise พร้อมระบุเหตุผล)"**

**Core Features & UI Requirements:**  
**General UI (Sleek & Premium Aesthetic):**

* **หน้า Login:** \* ผู้ใช้เข้าสู่ระบบด้วย **Username (รหัสพนักงาน)** และ **Password (รหัสผ่าน)**  
  * มี Validation กฏที่ว่า **"รหัสพนักงานในระบบห้ามซ้ำกันเด็ดขาด (Unique)"**  
  * ดีไซน์หน้า Login ต้อง Sleek มินิมอล อาจใช้พื้นหลังที่มีลูกเล่นเบลอ (Glassmorphism อ่อนๆ) หรือโทนสีสะอาดตา  
* **Sidebar & Layout:** เมนูด้านข้างแบบพับเก็บได้ (Collapsible Sidebar) พร้อมกับ Animation ที่ลื่นไหล (Smooth Transitions) ไม่มีอาการกระตุกเมื่อพับเก็บ การจัดวาง Layout เน้นพื้นที่ว่าง (Whitespace) ให้ระบบดูโปร่ง ไม่อึดอัด  
* **แจ้งเตือนงานรออนุมัติ (Pending Tasks):** ใน Sidebar หรือ Topbar ควรมี Badge แจ้งเตือนจำนวนรายการที่รอการอนุมัติ ที่ดูสวยงาม เนียนตา

**1\. หน้าจัดการหัวข้อตัวชี้วัด (KPI Master Data):**

* เฉพาะผู้จัดทำ (Assignee) เห็นปุ่ม "สร้าง/แก้ไข" และ "ส่งขออนุมัติ"  
* ผู้อนุมัติ (ตามระดับ) จะเห็นปุ่ม "อนุมัติ / ตีกลับ" เมื่อสถานะมาถึงตนเอง  
* มีหน้าแสดง Timeline หรือประวัติการอนุมัติ (ออกแบบ Timeline ให้ดู Modern และอ่านง่าย)  
* ฟิลด์ข้อมูลหลัก: เกณฑ์ชี้วัด, หน่วยวัด, น้ำหนัก(%), เป้าหมายระดับ 1-5, ผู้รับผิดชอบหลัก/ร่วม, แผนปฏิบัติการ (Initiative)

**2\. หน้าบันทึกและรายงานผลรายเดือน:**

* การแก้ไขข้อมูลทำได้โดย Assignee และต้องกด "ส่งขออนุมัติ" เป็นรายเดือน  
* **Top Action Bar & Filter:**  
  * Dropdown สำหรับเลือก **"เดือน และ ปี พ.ศ."** แบบ Sleek Design  
  * Tag สถานะการอนุมัติของเดือนนั้นๆ อย่างชัดเจน สวยงาม  
  * ปุ่ม Action สำหรับผู้อนุมัติ (Approve / Reject)  
* **Header Section:** แสดงชื่อผู้รับผิดชอบ, คำนิยาม โดยจัดวาง Typography (ตัวอักษร) ให้มี Hierarchy ชัดเจนและอ่านง่าย  
* **Performance Section:** ตารางแสดงผลเปรียบเทียบ (ใช้สไตล์ Table ที่สะอาด ขอบมน เส้นขอบบาง หรือไร้เส้นกั้นแนวตั้ง)  
* **Detail Input Section (Textareas):**  
  1. รายละเอียดผลการดำเนินงาน ประจำเดือน  
  2. แนวทางการดำเนินงาน (กรณีบรรลุระดับ 4\)  
  3. ปัญหาอุปสรรคหรือสาเหตุที่ทำให้ผลงานต่ำกว่าเป้า  
  4. แนวทางการแก้ไขให้ผลบรรลุเป้าหมายระดับ 5

**3\. หน้า Dashboard รวม:**

* **Global Date Filter:** ตัวกรอง เดือน และ ปี พ.ศ.  
* **สรุปสถานะการรายงาน:** นำเสนอผ่าน Data Visualization/Cards ที่ดูพรีเมียม มีเงาตกกระทบแบบนุ่มนวล (Soft shadows) และขอบมน (Rounded corners)  
* **ตารางผลการดำเนินงาน (Actual Performance):** แสดงผลงานล่าสุด ควบคู่กับ "หน่วยวัด" และ "สถานะการอนุมัติ" แบบเห็นแล้วเข้าใจง่ายทันที  
* **การเข้าดูและแก้ไขรายละเอียด:** คลิกที่รายการเพื่อไปยังหน้ารายละเอียด (/report/\[id\]) โดยใช้การเปลี่ยนหน้าแบบนุ่มนวล

**Technical Requirements & Stack:**

* **Frontend:** ใช้ **Next.js (App Router)** ร่วมกับ **TypeScript** (.ts, .tsx) เพื่อจัดการระบบ Routing อย่างเป็นระบบ (เช่น /dashboard, /kpi-master, /report/\[id\])  
* **Styling & UI Components:** \* ใช้ **Tailwind CSS** ร่วมกับ **shadcn/ui** สร้างสไตล์ **"Sleek Design"**  
  * คุมโทนสีน้ำเงิน/เทา/ม่วง ให้ดูแพงและเป็นทางการ (Premium Enterprise look)  
  * เน้นการใช้ ขอบมน (Rounded-xl/2xl), เงาที่นุ่มนวล (Drop-shadow-sm/md แบบบางๆ), แอฟเฟ็กต์ Hover ที่ลื่นไหล (Transitions duration-200 หรือ 300), และการใช้ Whitespace อย่างชาญฉลาด  
  * รองรับ Accessibility ที่ดีเยี่ยม  
* **Backend, Database & Authentication:** ใช้ **Supabase**  
  * จัดการระบบ Login ผ่าน Supabase (ออกแบบให้รองรับการ Login ด้วย รหัสพนักงาน อาจใช้ custom behavior ร่วมกับ Supabase Auth หรือสร้าง Table แยกสำหรับจัดการ Employee Login)  
  * ออกแบบ Schema ด้วย PostgreSQL (Tables สำหรับ Users, Roles, KPIs, Reports, Approval Logs)  
  * กำหนด Constraint UNIQUE ให้กับคอลัมน์รหัสพนักงาน (Employee ID) ในฐานข้อมูล  
  * ใช้ Row Level Security (RLS) เพื่อป้องกันไม่ให้ข้อมูลข้ามกองกัน (ยกเว้น Admin/ผู้รวบรวม)  
* **Deployment:** โครงสร้างโปรเจกต์ต้องพร้อมสำหรับการ Deploy ขึ้น **Vercel**